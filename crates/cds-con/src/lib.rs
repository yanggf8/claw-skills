//! Library half of the cds-con skill.
//!
//! Analysis and rendering for the daily attribute-2 push. `cost` mirrors
//! finance-cli's `cost level`, the authority for that rule. Run/markers
//! implement plan decision 7: the skill status reports the **run**, not the data.

pub mod cost;
pub mod render;
pub mod run;
